import Editorial from './Editorial'
import './Zine.css'

/**
 * ZINE — the Editorial structure with a poster's shape language:
 * square corners, thick rules, tilted cards and section titles set on
 * accent slabs. Same markup, different attitude.
 */
export default function Zine() {
  return <Editorial variant="zine" />
}
